// export const requiredInput = (input) =>
//    input ? undefined : (console.log("Требуется ввод"));

// export const matchInput = (input, input) =>
//
//    input === input.password ? undefined : (console.log('Пароль не совпадает'));
